import type { ReactNode } from 'react'

export default function PackageLayout (
  { children } : {children: ReactNode}
) {
  // TODO: Add the sub navigation for packages

  return children
}
