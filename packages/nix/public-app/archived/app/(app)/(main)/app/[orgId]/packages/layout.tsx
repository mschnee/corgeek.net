'use client'

import type { ReactNode } from 'react'

export default function PackagesLayout (
  { children } : {children: ReactNode}
) {
  // TODO: Add a screen for when the package namespace is not set

  return children
}
