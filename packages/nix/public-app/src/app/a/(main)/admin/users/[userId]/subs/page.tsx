'use client'

import React, { memo } from 'react'

/************************************************
 * Root
 * **********************************************/

interface PageProps {
  params: {userId: string;}
}
export default memo(function Page (_: PageProps) {
  return <div>TODO</div>
})
